{
  "name": "gcp-logs",
  "version": "1.0.0",
  "description": "Package that standardizes logs for GCP",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.4.3",
    "@types/node": "^18.0.0"
  }
}
